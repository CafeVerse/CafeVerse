import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.vikumkarunathilake.cafeverse',
  appName: 'CaféVerse',
  webDir: 'out/renderer'
}

export default config
